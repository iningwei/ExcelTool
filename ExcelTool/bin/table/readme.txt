1.数值类型：int,uint,int[],uint[],string,float

2.表格填写数组类型的写法有2种，标准的为”[1,2,3]“，另一种写法为”1|2|3“（导表工具会转换为”[1,2,3]“）；填写的表格为空的时候，数值类型会自动转换为0，字符串会转换为”“

3.必有字段ID必须写成ID，不可以写成Id、id，数值类型必须为int

4.导出类型：0-客服端和服务器（默认不填），1-仅导出到客户端，2-仅导出到服务器，3-不导出